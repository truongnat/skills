# /w-arch-review — Run the architecture review workflow

You are executing the **arch-review** workflow (**file** [`workflows/dev/w-arch-review.md`](../../workflows/dev/w-arch-review.md)).

## User input

$ARGUMENTS

## Instructions

1. **Read** [`workflows/dev/w-arch-review.md`](../../workflows/dev/w-arch-review.md) in full.
2. **Resolve inputs:** `design_artifact` (required); optional `constraints`, `domain_stack`.
3. **Execute** steps; use [`templates/report/architecture-decision-record.md`](../../templates/report/architecture-decision-record.md) when recording decisions.

## Rules

- If the artifact is too vague, return **questions** before judging.

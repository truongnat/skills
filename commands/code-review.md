---
name: /code-review
targets: [cursor, claude]
description: |
  code-review workflow — routes to specialized agent skills
  
  Run `/code-review` to start the workflow.
---

# /code-review Command

Triggers the `code-review` workflow defined in `workflows/dev/code-review.md`.

Execute the workflow steps in order, gathering inputs, running analysis, and producing a structured report.


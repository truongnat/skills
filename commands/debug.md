---
name: /debug
targets: [cursor, claude]
description: |
  debug workflow — routes to specialized agent skills
  
  Run `/debug` to start the workflow.
---

# /debug Command

Triggers the `debug` workflow defined in `workflows/dev/debug.md`.

Execute the workflow steps in order, gathering inputs, running analysis, and producing a structured report.


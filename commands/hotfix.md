---
name: /hotfix
targets: [cursor, claude]
description: |
  hotfix workflow — routes to specialized agent skills
  
  Run `/hotfix` to start the workflow.
---

# /hotfix Command

Triggers the `hotfix` workflow defined in `workflows/dev/hotfix.md`.

Execute the workflow steps in order, gathering inputs, running analysis, and producing a structured report.


---
name: /incident
targets: [cursor, claude]
description: |
  incident workflow — routes to specialized agent skills
  
  Run `/incident` to start the workflow.
---

# /incident Command

Triggers the `incident` workflow defined in `workflows/dev/incident.md`.

Execute the workflow steps in order, gathering inputs, running analysis, and producing a structured report.


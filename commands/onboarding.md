---
name: /onboarding
targets: [cursor, claude]
description: |
  onboarding workflow — routes to specialized agent skills
  
  Run `/onboarding` to start the workflow.
---

# /onboarding Command

Triggers the `onboarding` workflow defined in `workflows/dev/onboarding.md`.

Execute the workflow steps in order, gathering inputs, running analysis, and producing a structured report.


---
name: /perf-investigation
targets: [cursor, claude]
description: |
  perf-investigation workflow — routes to specialized agent skills
  
  Run `/perf-investigation` to start the workflow.
---

# /perf-investigation Command

Triggers the `perf-investigation` workflow defined in `workflows/dev/perf-investigation.md`.

Execute the workflow steps in order, gathering inputs, running analysis, and producing a structured report.


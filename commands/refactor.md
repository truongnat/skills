---
name: /refactor
targets: [cursor, claude]
description: |
  refactor workflow — routes to specialized agent skills
  
  Run `/refactor` to start the workflow.
---

# /refactor Command

Triggers the `refactor` workflow defined in `workflows/dev/refactor.md`.

Execute the workflow steps in order, gathering inputs, running analysis, and producing a structured report.


---
name: /release
targets: [cursor, claude]
description: |
  release workflow — routes to specialized agent skills
  
  Run `/release` to start the workflow.
---

# /release Command

Triggers the `release` workflow defined in `workflows/dev/release.md`.

Execute the workflow steps in order, gathering inputs, running analysis, and producing a structured report.


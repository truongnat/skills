---
name: /security-audit
targets: [cursor, claude]
description: |
  security-audit workflow — routes to specialized agent skills
  
  Run `/security-audit` to start the workflow.
---

# /security-audit Command

Triggers the `security-audit` workflow defined in `workflows/dev/security-audit.md`.

Execute the workflow steps in order, gathering inputs, running analysis, and producing a structured report.


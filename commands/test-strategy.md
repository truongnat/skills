---
name: /test-strategy
targets: [cursor, claude]
description: |
  test-strategy workflow — routes to specialized agent skills
  
  Run `/test-strategy` to start the workflow.
---

# /test-strategy Command

Triggers the `test-strategy` workflow defined in `workflows/dev/test-strategy.md`.

Execute the workflow steps in order, gathering inputs, running analysis, and producing a structured report.


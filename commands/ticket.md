---
name: /ticket
targets: [cursor, claude]
description: |
  ticket workflow — routes to specialized agent skills
  
  Run `/ticket` to start the workflow.
---

# /ticket Command

Triggers the `ticket` workflow defined in `workflows/dev/ticket.md`.

Execute the workflow steps in order, gathering inputs, running analysis, and producing a structured report.


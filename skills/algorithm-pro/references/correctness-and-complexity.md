# Correctness and complexity

- State invariant(s) and why each step preserves them.
- Use proof sketch style aligned to algorithm type (induction, exchange argument, cut property).
- Derive time and space complexity from dominant operations and data structures.
- Call out hidden assumptions (sorted input, non-negative weights, bounded values).
- Document worst-case behavior and practical expected-case notes separately.

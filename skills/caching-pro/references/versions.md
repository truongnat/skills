# Caching — versions

## HTTP

- **CDN** product features (stale-while-revalidate, early hints) vary by vendor/version.

## Redis/Memcached

- **Eviction policy** and **TLS** defaults differ by major version.

## App frameworks

- Next.js **fetch** cache defaults changed across majors — align with **`nextjs-pro`**.

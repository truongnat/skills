# CLI — versions

## Semver for published CLIs

- Treat argv and exit codes as **public API**.

## Shell completion

- Generator version tied to parser library major.

## OS

- Windows `conhost` vs Unix TTY — test quoting and paths.

## argv as public API

- Renamed flags / removed defaults → treat as **semver** for published tools — communicate in CHANGELOG.

# Deployment — versions

## Platform APIs

- K8s minor, Actions `actions/*` major — pin for reproducibility.

## Serverless runtimes

- Node/Python runtime EOL on cloud — track deprecation.

## IaC

- Terraform/provider lock — state and drift sensitivity.

# Tips and tricks

- **Copy** images before in-place ops (`thumbnail`) when you need the original for later steps.
- **sRGB** workflow for web — avoid accidental double gamma when compositing.
- **Batch** folders: `Path.glob` + consistent naming; avoid overwriting sources.

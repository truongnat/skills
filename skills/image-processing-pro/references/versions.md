# Image processing — versions

## Pillow

- **Major** releases change resampling defaults and format support — pin in `requirements` / `pyproject`.

## Python

- Binary wheels vs source build for image codecs.

## Format support

- **HEIC/WebP** availability depends on build flags and optional deps.

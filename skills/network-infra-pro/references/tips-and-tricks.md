# Tips and tricks

- Use consistent naming for network objects (`env-zone-purpose-index`).
- Keep an always-updated connectivity matrix for critical services.
- Make small, reversible route and policy changes during low-risk windows.
- Validate DNS and cert renewal paths before expiry windows.
- Use canary targets or weighted routes before full traffic cutover.

# Network infra — versions

## Cloud APIs

- VPC/LB API behavior differs by provider version — pin IaC modules.

## TLS

- Minimum protocol/cipher policy; cert lifetime (short-lived ACME).

## K8s

- Ingress controller version affects annotations and path behavior.

# Integration map — performance-tuning-pro

| Combined skill | Why | This skill owns | Other skill owns |
|----------------|-----|-----------------|------------------|
| **`postgresql-pro`** | Query latency | When to index, batch, paginate | SQL, RLS, migrations |
| **`caching-pro`** | Throughput | Cache layers, invalidation | Redis/CDN mechanics |
| **`testing-pro`** | Regressions | Perf budgets in CI | Test layout |
| **`nestjs-pro`** / **`nextjs-pro`** | Stack-specific hot paths | Measurement hooks | Framework APIs |
| **`docker-pro`** / **`deployment-pro`** | Runtime limits | Container CPU/mem, scaling knobs | Infra |

# Integration map — react-pro

| Combined skill | Why | `react-pro` owns | Other skill owns |
|----------------|-----|------------------|------------------|
| **`nextjs-pro`** | RSC, routing, caching | Hooks, client UI, hydration issues in client trees | App Router layout, `fetch` cache, Server Actions, middleware |
| **`testing-pro`** | RTL/Jest/Vitest | Component behavior, hooks testing strategy | CI wiring, pyramid, flakiness policy |
| **`security-pro`** | XSS, CSP | Safe JSX patterns, avoiding `dangerouslySetInnerHTML` | Threat model, headers, token policy |
| **`typescript-pro`** | TSX types | Prop/component typing ergonomics | `tsconfig`, advanced generics |
| **`design-system-pro`** | Consistency | Composition with design tokens in React | Visual language, tokens source of truth |

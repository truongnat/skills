# Repo tooling — versions

## `dist/tools.js`

- Built from TypeScript in this repo — version CLI behavior with git tag or `package.json`.

## Node / npm

- Match repo `engines` for supportability.

## Embedding model

- If model or dims change, full **re-embed** may be required — note in runbooks.

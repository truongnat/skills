# Stream / RTC — versions

## Browser WebRTC

- **`RTCPeerConnection`** API surface evolves; test **Safari** separately.
- **Insertable streams**, **Encoded Transform** — feature-detect.

## Server SFU vendors

- Pin **server** and **client SDK** versions; churn in simulcast/SVC defaults.

## Codec support

- **H.264** patent/royalty and **AV1** decode availability differ by device — maintain fallback ladder.

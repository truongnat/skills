# Tauri — versions

## Tauri 2

- **Capabilities** replace many v1 allowlist patterns — migrate docs when upgrading.
- **Plugins** namespace and feature flags changed — verify each plugin’s README for major.

## Rust

- Match **MSRV** in `Cargo.toml` with team toolchain; `rust-version` field for clarity.

## WebView

- **Windows** WebView2 runtime distribution strategy (fixed vs evergreen).
- **Linux** WebKitGTK version floor for target distros.

## Mobile (beta/experimental areas)

- Track Tauri mobile docs separately; APIs move faster than desktop.

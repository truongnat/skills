# TypeScript — version notes

| TS version | Highlights to double-check |
|------------|----------------------------|
| **5.x** | `const` type parameters, improved `satisfies`, decorator semantics when enabled |
| **4.x** | `satisfies` introduced; template literal improvements |

Always read [release notes](https://www.typescriptlang.org/docs/handbook/release-notes/overview.html) for your **exact** compiler version in `package.json`.
